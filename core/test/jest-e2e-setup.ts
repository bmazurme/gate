// Должен выполниться до того, как ConfigModule прочитает .env: dotenv не
// переопределяет уже выставленные переменные, поэтому e2e-тесты используют
// отдельную БД и не задевают данные для локальной разработки.
process.env.DB_NAME = 'gate_test';
